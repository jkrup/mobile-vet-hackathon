!!!
%html
  %head
    %title MobileVet
    = stylesheet_link_tag    "application", :media => "all"
    = javascript_include_tag "application"
    = csrf_meta_tags
  %body
    - if current_user.nil?
      %p NOT LOGGED IN YO
    - else
      %p
        Logged in as
        = current_user.first_name
        = current_user.last_name
        = current_user.id
      %p
        %a{href: "/requests/show"} "make request"
        %a{href: "/"} "home"
    = yield
